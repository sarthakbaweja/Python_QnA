[pytest]
testpaths = tests rag/tests
markers =
    integration: marks tests requiring live services (Qdrant)
    eval: marks quality eval tests (requires live Qdrant + indexed data; RAGAS tests also need GROQ_API_KEY)
